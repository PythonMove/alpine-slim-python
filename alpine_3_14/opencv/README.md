# alpine-python3-opencv
Slim Docker image for opencv python package.

# Build
```
git clone https://github.com/PythonMove/alpine-python3-opencv.git
cd alpine-python3-opencv
sudo docker build -t <name>:<tag> .
```
